package sekelsta.game.entity;

import sekelsta.game.Game;
/*
public class AI implements Controller  {
    public void update(Mob mob) {
        int amount = 4;
        int x = Game.RANDOM.nextInt(amount + 1) - amount / 2;
        int y = Game.RANDOM.nextInt(amount + 1) - amount / 2;
        int z = Game.RANDOM.nextInt(amount + 1) - amount / 2;
        mob.addVelocity(x, y, z);
    }
}*/
